# OS / エディタ
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
__pycache__/

# 公式ガイド等の PDF はリポジトリに含めない（著作権物。ローカルには残す）
*.pdf

# ローカル実行時の DB（ブラウザクリアには強い）。git バックアップが要るときは
# アプリの「記録をJSON保存」で dea_state.json を書き出して、それを commit する。
quiz.sqlite
dea_state.imported-*.json
